# 标签：序数优先级（Ordinal-Priorities）

Super Productivity 没有“序数优先级”的概念。也就是说，没有办法将任务标记为“高-低”或“1 到 5”，并按该值排序。

通过对任务打标签，你可以近似实现此功能。对于某些标签类型，你可以使用前缀以便更轻松地过滤它们。例如，你的标签可能如下所示：

- `#P1 #P2 #P3 #P4`（P 表示 priority / 优先级）
- `#I-LOW #I-MED #I-HIGH`（I 表示 importance / 重要性）
